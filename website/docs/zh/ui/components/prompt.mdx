---
description: Prompt 组件用于突出展示可复制、可折叠的 Agent 指令块。
overviewHeaders: []
tag: new
---

import { Prompt } from '@rspress/core/theme';

# Prompt

`Prompt` 组件用于突出展示可复用的指令内容，读者可以直接复制并粘贴给自己的 AI 工具。

## 用法

在 MDX 中使用前，先引入 `Prompt`：

```mdx title="index.mdx"
import { Prompt } from '@rspress/core/theme';

<Prompt
  title="一键创建 Rspress 项目"
  description="复制这段 Prompt 发送给你的 AI Agent，它会自动帮你搭建一个全新的 Rspress 站点。"
>
  按照官方快速开始指南 https://rspress.rs/guide/start/getting-started.html
  创建一个 Rspress 项目。使用 `create rspress@latest` 初始化项目、安装依赖、
  启动开发服务器，并确认页面正常渲染。
</Prompt>
```

<Prompt
  title="一键创建 Rspress 项目"
  description="复制这段 Prompt 发送给你的 AI Agent，它会自动帮你搭建一个全新的 Rspress 站点。"
>
  <p>
    按照官方快速开始指南{' '}
    <a href="https://rspress.rs/guide/start/getting-started.html">
      rspress.rs/guide/start/getting-started.html
    </a>{' '}
    创建一个 Rspress 项目。
  </p>
  <ul>
    <li>使用 `create rspress@latest` 初始化项目。</li>
    <li>安装依赖。</li>
    <li>启动开发服务器。</li>
    <li>确认页面正常渲染。</li>
  </ul>
</Prompt>

Prompt 内容可以折叠，复制按钮在折叠和展开状态下都可以正常工作。

## 自定义

你可以 eject `Prompt` 组件来自定义其样式和行为：

```bash
npx rspress eject Prompt
```

这会将组件复制到 `theme/components/Prompt`。自定义后，在 `theme/index.tsx` 中重新导出：

```tsx title="theme/index.tsx"
export { Prompt } from './components/Prompt';
export * from '@rspress/core/theme-original';
```

## Props

```ts
interface PromptProps extends React.HTMLAttributes<HTMLDivElement> {
  /**
   * 在标题区显示的描述文本。
   */
  description?: string;
  /**
   * Prompt 区块的标题。
   * @default 'Agent Prompt'
   */
  title?: string;
  /**
   * 标题上方显示的 eyebrow 标签。
   * @default 'For your agent'
   */
  eyebrow?: string;
  /**
   * 控制初始折叠状态。
   * @default true
   */
  defaultCollapsed?: boolean;
  /**
   * 自定义复制到剪贴板的文本。
   */
  copyText?: string;
  children: React.ReactNode;
}
```
