# Конфигурация метаданных

В этом документе описано, как с помощью метаданных настраивать различные свойства страницы: заголовок, описание, тип страницы, навигацию и т. д.

Что такое метаданные (`frontmatter`) и как ими пользоваться — см. в разделе [Метаданные](/guide/use-mdx/frontmatter).

## title

- Тип: `string`

Заголовок страницы. По умолчанию в качестве заголовка HTML-документа используется заголовок первого уровня (H1) страницы. Если нужно задать другой заголовок, можно указать его через метаданные. Например:

```yaml
---
title: Моя главная страница
---
```

## description

- Тип: `string`

Пользовательское описание страницы. Например:

```yaml
---
description: Это моё собственное описание для данной страницы.
---
```

## pageType

- Тип: `'home' | 'doc' | 'doc-wide' | 'custom' | 'blank' | '404'`
- По умолчанию: `'doc'`

Тип страницы. По умолчанию используется тип `doc`. Если требуется другой тип, его можно задать через поле `pageType` в метаданных. Например:

```yaml
---
pageType: home
---
```

Каждое значение `pageType` означает следующее:

import PageType from '@ru/fragments/page-type.mdx';

<PageType />

## titleSuffix

- Тип: `string`

Устанавливает суффикс заголовка страницы. Если `titleSuffix` не задан, по умолчанию в качестве суффикса используется [title](/api/config/config-basic#title) сайта.

```yaml
---
titleSuffix: 'Генератор статических сайтов на базе Rsbuild'
---
```

По умолчанию между заголовком и суффиксом используется разделитель `-`, но вы также можете использовать `|`:

```yaml
---
titleSuffix: '| Генератор статических сайтов на базе Rsbuild'
---
```

## head

- Тип: `[string, Record<string, string>][]`

Указывает дополнительные теги `<head>`, которые будут внедрены именно для текущей страницы. Добавляются после тегов, внедрённых на уровне конфигурации всего сайта.

Например, с их помощью можно задать кастомные мета-теги для [Open Graph](https://ogp.me/).

```yaml
---
head:
  - - meta
    - property: og:url
      content: https://example.com/foo/
  - - meta
    - property: og:image
      content: https://example.com/bar.jpg
# - - [htmlTag]
#   - [attributeName]: [attributeValue]
#     [attributeName]: [attributeValue]
---
```

Полученные теги head будут следующими:

```html
<head>
  <meta property="og:url" content="https://example.com/foo/" />
  <meta property="og:image" content="https://example.com/bar.jpg" />
</head>
```

## hero

- Тип: `Object`

Конфигурация блока hero (главного баннера) на домашней странице. Имеет следующую структуру:

```ts
interface Hero {
  name: string;
  text: string;
  tagline: string;
  image?: {
    src: string | { dark: string; light: string };
    alt: string;
    /**
     * `srcset` и `sizes` — это атрибуты тега `<img>`. Подробности использования см. на https://mdn.io/srcset.
     * Если значение передаётся массивом, Rspress объединит элементы массива запятыми.
     **/
    srcset?: string | string[];
    sizes?: string | string[];
  };
  actions: {
    text: string;
    link: string;
    theme: 'brand' | 'alt';
  }[];
}
```

Например, с помощью следующих метаданных можно задать конфигурацию hero-блока страницы:

```yaml
---
pageType: home

hero:
  name: Rspress
  text: Решение для документации
  tagline: Современный технологический стек для разработки документации
  actions:
    - theme: brand
      text: Введение
      link: /ru/guide/introduction
    - theme: alt
      text: Быстрый старт
      link: /ru/guide/getting-started
---
```

При задании `hero.text` можно использовать символ `|` в YAML, чтобы вручную управлять переносами строк:

```yaml
---
pageType: home

hero:
  name: Rspress
  text: |
    Решение для
    документации
```

Или можно использовать `HTML` для задания hero-конфигурации страницы:

```yaml
---
pageType: home

hero:
  name: <span class="hero-name">Rspress</span>
  text: <span class="hero-text">Решение для документации</span>
  tagline: <span class="hero-tagline">Современный технологический стек для разработки документации</span>
  actions:
    - theme: brand
      text: <span class="hero-actions-text">Введение</span>
      link: /ru/guide/introduction
    - theme: alt
      text: <span class="hero-actions-text">Быстрый старт</span>
      link: /ru/guide/getting-started
---
```

## features

- Тип: `Array`
- По умолчанию: `[]`

Конфигурация блока `features` (функции, фичи, возможности) для страницы `home`. Имеет следующие типы:

```ts
interface Feature {
  title: string;
  details: string;
  icon: string;
  // Длина сетки карточек, в настоящее время поддерживаются только [3, 4, 6]
  span?: number;
  // Ссылка, не обязательна.
  link?: string;
}

export type Features = Feature[];
```

Например, вы можете использовать следующие метаданные для указания списка возможностей вашего продукта на странице `home`:

```yaml
---
pageType: home

features:
  - title: 'Поддержка MDX'
    details: MDX — это мощный способ написания контента. Вы можете использовать компоненты React в Markdown.
    icon: 📦
  - title: 'Богатый функционал'
    details: Готовая поддержка i18n, полнотекстовый поиск и т. д.
    icon: 🎨
  - title: 'Настраиваемый'
    details: Вы можете настроить тему интерфейса и процесс сборки.
    icon: 🚀
---
```

## sidebar

Показывать ли боковую панель слева. По умолчанию страница `doc` отображает боковую панель слева. Если вы хотите скрыть боковую панель слева, вы можете использовать следующую опцию:

```yaml
---
sidebar: false
---
```

## outline

Отображать ли колонку с оглавлением справа. По умолчанию страница `doc` отображает колонку с оглавлением справа. Вы можете скрыть колонку с оглавлением с помощью следующей опции:

```yaml
---
outline: false
---
```

## footer

Отображать ли компоненты в нижней части документа (такие как предыдущая/следующая страница). По умолчанию страница `doc` отображает нижний колонтитул. Вы можете скрыть нижний колонтитул с помощью следующей опции:

```yaml
---
footer: false
---
```

## navbar

Скрывать ли верхнюю панель навигации. Вы можете скрыть верхнюю панель навигации с помощью следующей опции:

```yaml
---
navbar: false
---
```

## overviewHeaders

- Тип: `number[]`
- По умолчанию: `[2]`

Заголовки, отображаемые на странице обзора. По умолчанию отображается заголовок h2. Но если вы хотите отображать другие заголовки, вы можете указать это с помощью мета-поля `overviewHeaders`. Например:

```yaml
---
overviewHeaders: []
---
```

Или

```yaml
---
overviewHeaders: [2, 3]
---
```

## context

- Тип: `string`

После настройки атрибут `data-context` будет добавлен к DOM-узлу при генерации боковой панели, а значением будет заданная конфигурация.

```yaml title="foo.mdx"
---
context: 'context-foo'
---
```

```yaml title="bar.mdx"
---
context: 'context-bar'
---
```

Структура DOM окончательно сгенерированной боковой панели сокращённо выглядит следующим образом:

```html
<div class="rspress-sidebar-group">
  <div className="rspress-sidebar-item" data-context="context-foo"></div>
  <div className="rspress-sidebar-item" data-context="context-bar"></div>
</div>
```
