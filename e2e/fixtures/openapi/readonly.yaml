openapi: 3.1.0
info:
  title: Read-only API
  version: 1.0.0
servers: []
security:
  - token: []
paths:
  /status:
    get:
      operationId: status
      summary: Get status
      responses:
        '200':
          description: OK
components:
  securitySchemes:
    token:
      type: http
      scheme: bearer
